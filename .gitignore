# Secrets — never commit
.env
.env.*
*.env
!.env.example

# Bruno — local.bru holds the real NIWA API key; local.example.bru is the
# committed template with a placeholder
bruno/**/environments/local.bru

# Superpowers SDD scratch workspace (ledgers, briefs, review packages)
.superpowers/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Node (no package.json today, but wrangler/npx can create these)
node_modules/
.wrangler/
